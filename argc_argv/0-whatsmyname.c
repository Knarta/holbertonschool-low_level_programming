#include "main.h"

/**
 * main - Program entry point
 *
 * @argc: total argument number
 *
 * @argv: argument list
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	if (argc != 1)
		return (0);

	printf("%s\n", argv[0]);

	return (0);
}
